@model List<demo.Models.CartItem>

<div class="cart">
    <p>
        Chào mừng bạn đến với website <span>Cart:</span><div id="dd" class="wrapper-dropdown-2">
            @Model.Count sản phẩm - @(Model.Sum(x=>x.Product.Price * x.Product.Quantity))
            <ul class="dropdown">
                <li>you have no items in your Shopping cart</li>
            </ul>
        </div>
    </p>
</div>